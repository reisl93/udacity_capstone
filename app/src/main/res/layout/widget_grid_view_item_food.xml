<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/widget_tv_portion"
        style="@style/widget_text"
        tools:text="150"
        android:layout_gravity="end"
        android:textAlignment="textEnd"
        android:layout_width="40dp" />

    <TextView
        android:id="@+id/widget_tv_measure"
        style="@style/widget_text"
        tools:text="G"
        android:layout_gravity="start"
        android:textAlignment="textStart"
        android:layout_width="10dp" />

    <TextView
        android:layout_marginLeft="@dimen/half_grid_size"
        android:layout_marginStart="@dimen/half_grid_size"
        tools:text="My awesome organge juice"
        android:id="@+id/widget_tv_name"
        style="@style/widget_text"
        android:layout_width="wrap_content" />
</LinearLayout>